{
    "name": "core",
    "version": "0.1.0",
    "private": true,
    "devDependencies": {
        "ganache-cli": "^6.12.1"
    },
    "scripts": {
        "test": "truffle test",
        "compile": "truffle compile",
        "a1": "truffle migrate --network harmony_testnet",
        "ganache": "ganache-cli --mnemonic $(grep MNEMONIC .env | cut -d '=' -f2)"
    },
    "dependencies": {
        "dotenv": "^10.0.0",
        "replace-in-file": "^6.1.0",
        "@truffle/hdwallet-provider": "^1.4.0",
        "web3": "^1.3.1",
        "web3-abi-helper": "^4.2.3"
    }
}
